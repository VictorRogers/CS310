/Victor Rogers
/CS310
/Program 1.2
		ORG		100
		Input			/User enters upper bound
		Add		One	/Add one for indexing
		Store		Y	/Store upper bound
		Load		One	/Load the constant one
		Store		X	/Initialize counter variable X
Loop,		Load		X	/Load X
		Subt		Y	/Compare to upper bound
		SkipCond	000	/If AC < 0 (X is less than Y), remain in loop
		Jump		Endloop	/If not, then exit the loop
		Load		Sum
		Add		X	/Add X to Sum
		Store		Sum	/Store result in Sum
		Load		X
		Add		One	/Increment counter variable X
		Store		X
		Jump		Loop
Endloop,	Load		Sum
		Output			/Print Sum
		Halt			/End program
Sum,		Dec		0
X,		Dec		0	
One,		Dec		1	/Constant 1
Y,		Dec		0	/User defined upper bound
		End
